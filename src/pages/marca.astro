---
import Layout from '@/layouts/Layout.astro'
import Container from '@/components/Container.astro'

const brands = [
  {
    title: 'Logotipo Principal',
    description:
      'El logotipo oficial de JSConf España 2026. Utilízalo en fondos claros o donde la visibilidad sea óptima.',
    image: '/logo.png',
    svg: '/favicon.svg',
    png: '/logo.png',
  },
  {
    title: 'Logotipo Secundario',
    description:
      'Versión en blanco y negro para usos específicos donde se requiera sobriedad o el fondo no permita el uso del color original.',
    image: '/logo-bw.png',
    svg: '/logo-bw.svg',
    png: '/logo-bw.png',
    bg: 'bg-js-yellow',
  },
]

const colors = [
  { name: 'JS Yellow', hex: '#F7DF1E', bg: 'bg-[#F7DF1E]', text: 'text-black' },
  { name: 'JS Black', hex: '#000000', bg: 'bg-[#000000]', text: 'text-white' },
]
---

<Layout title="Marca y Recursos">
  <main class="pt-32 pb-20">
    <Container>
      <header class="text-center mb-12">
        <h1 class="text-4xl md:text-5xl font-bold font-display mb-5 text-white uppercase tracking-tighter">
          Marca y Recursos
        </h1>
        <p class="text-gray-400 max-w-2xl mx-auto text-lg">
          Descarga los archivos oficiales y consulta las guías de estilo para representar correctamente la JSConf España 2026.
        </p>
      </header>

      <!-- Logotipos -->
      <section class="mb-32">
        <h2 class="text-3xl font-bold mb-10 flex items-center gap-4">
          <span class="text-js-yellow">01.</span> Logotipos
        </h2>

        <div class="grid grid-cols-1 md:grid-cols-2 gap-10">
          {
            brands.map((brand) => (
              <div class="bg-white/5 border border-white/10 rounded-2xl p-8 flex flex-col items-center">
                <div
                  class:list={[
                    'p-12 rounded-xl mb-8 flex items-center justify-center min-h-[200px] w-full',
                    brand.bg || 'bg-white',
                  ]}
                >
                  <img src={brand.image} alt={brand.title} class="max-h-24 w-auto object-contain" />
                </div>
                <div class="text-center md:text-left w-full">
                  <h3 class="text-2xl font-bold mb-4">{brand.title}</h3>
                  <p class="text-white/60 mb-8">{brand.description}</p>
                  <div class="flex flex-wrap gap-4">
                    <a
                      href={brand.svg}
                      download
                      class="inline-flex items-center justify-center gap-2 px-6 py-3 font-bold uppercase tracking-wide text-xs bg-js-yellow text-js-black border-2 border-js-yellow rounded hover:bg-transparent hover:text-js-yellow transition-all"
                    >
                      Descargar SVG
                    </a>
                    <a
                      href={brand.png}
                      download
                      class="inline-flex items-center justify-center gap-2 px-6 py-3 font-bold uppercase tracking-wide text-xs border-2 border-white/20 text-white rounded hover:bg-white hover:text-black transition-all"
                    >
                      Descargar PNG
                    </a>
                  </div>
                </div>
              </div>
            ))
          }
        </div>
      </section>

      <!-- Colores -->
      <section class="mb-32">
        <h2 class="text-3xl font-bold mb-10 flex items-center gap-4">
          <span class="text-js-yellow">02.</span> Colores
        </h2>
        <div class="grid grid-cols-2 md:grid-cols-4 gap-6">
          {
            colors.map((color) => (
              <div class="space-y-4">
                <button
                  data-color={color.hex}
                  class="color-card group relative w-full aspect-square rounded-2xl overflow-hidden shadow-lg border border-white/10 transition-all active:scale-95 cursor-pointer selection:bg-transparent"
                  title="Click para copiar el color"
                >
                  <div class={`${color.bg} w-full h-full`} />
                  <div class="absolute inset-0 bg-black/40 opacity-0 group-hover:opacity-100 transition-opacity flex items-center justify-center">
                    <span class="text-white font-bold text-sm uppercase tracking-widest">Copiar HEX</span>
                  </div>
                  <div class="absolute bottom-4 left-4 font-bold text-xl mix-blend-difference text-white">
                    {color.hex}
                  </div>
                </button>
                <div>
                  <h4 class="font-bold">{color.name}</h4>
                  <p class="text-white/40 text-sm italic select-all cursor-text">{color.hex}</p>
                </div>
              </div>
            ))
          }
        </div>
      </section>

      <!-- Guías de uso -->
      <section>
        <h2 class="text-3xl font-bold mb-10 flex items-center gap-4">
          <span class="text-js-yellow">03.</span> Guías de uso
        </h2>
        <div class="grid grid-cols-1 md:grid-cols-2 gap-12">
          <div class="bg-green-500/10 border border-green-500/20 p-8 rounded-2xl">
            <h3 class="text-xl font-bold mb-6 text-green-400 flex items-center gap-2">
              <i class="fas fa-check-circle"></i> Qué hacer
            </h3>
            <ul class="space-y-4 text-white/70">
              <li class="flex gap-3">
                <span class="text-green-500">•</span>
                Deja suficiente espacio libre alrededor del logo.
              </li>
              <li class="flex gap-3">
                <span class="text-green-500">•</span>
                Asegúrate de que haya buen contraste entre el logo y el fondo.
              </li>
              <li class="flex gap-3">
                <span class="text-green-500">•</span>
                Utiliza las versiones oficiales proporcionadas arriba.
              </li>
            </ul>
          </div>

          <div class="bg-red-500/10 border border-red-500/20 p-8 rounded-2xl">
            <h3 class="text-xl font-bold mb-6 text-red-400 flex items-center gap-2">
              <i class="fas fa-times-circle"></i> Qué NO hacer
            </h3>
            <ul class="space-y-4 text-white/70">
              <li class="flex gap-3">
                <span class="text-red-500">•</span>
                No cambies los colores ni la tipografía del logotipo.
              </li>
              <li class="flex gap-3">
                <span class="text-red-500">•</span>
                No estires, rotes ni distorsiones la imagen.
              </li>
              <li class="flex gap-3">
                <span class="text-red-500">•</span>
                No añadas sombras ni efectos adicionales innecesarios.
              </li>
            </ul>
          </div>
        </div>
      </section>
    </Container>
  </main>
</Layout>

<script>
  const colorCards = document.querySelectorAll('.color-card')

  colorCards.forEach(card => {
    card.addEventListener('click', () => {
      const color = card.getAttribute('data-color')
      if (color) {
        navigator.clipboard.writeText(color)

        // Efecto visual de copiado
        const originalText = (card.querySelector('span') as HTMLElement)?.innerText
        const span = card.querySelector('span') as HTMLElement
        if (span) {
          span.innerText = '¡Copiado!'
          setTimeout(() => {
            span.innerText = originalText || 'Copiar HEX'
          }, 2000)
        }
      }
    })
  })
</script>
